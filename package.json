{
  "name": "gsd-test",
  "version": "0.1.0",
  "private": true,
  "description": "Server-rendered HTMX/SSE clock powered by Rust WebAssembly",
  "scripts": {
    "build:wasm": "CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_LINKER=wasm-ld cargo build --manifest-path rust/clock-wasm/Cargo.toml --target wasm32-unknown-unknown --release && nix run nixpkgs#wasm-bindgen-cli -- --target bundler --out-dir rust/clock-wasm/pkg --out-name clock_wasm rust/clock-wasm/target/wasm32-unknown-unknown/release/clock_wasm.wasm",
    "test:rust": "cargo test --manifest-path rust/clock-wasm/Cargo.toml",
    "test": "pnpm run build:wasm && pnpm run test:rust && just test-live",
    "dev": "wrangler dev --local --live-reload --show-interactive-dev-session=true"
  }
}
